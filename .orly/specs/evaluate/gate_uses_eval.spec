evidence: src/gate.ts, src/evaluate.ts

Look at `src/gate.ts` and `src/evaluate.ts` under `project.files`, their real current content. Does the Stop-time gate obtain the requirement statuses by calling the same evaluate function that `orly eval` uses, and block on a violated requirement before asking any turn question?
