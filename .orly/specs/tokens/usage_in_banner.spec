evidence: src/orly.ts

Look at the `src/orly.ts` entry under `project.files`, which is the file's real current content. Does the status line it builds after a judgment include the judge's input and output token counts from the response's `usage`?
