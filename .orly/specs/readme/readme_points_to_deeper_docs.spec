cut: 0.75
evidence: README.md, llms.txt
fitted: met>=0.84, no in-state negative (state: transcript + file evidence; .orly at the repository root)

Look at the `README.md` entry under `project.files`. Does it contain markdown links whose targets are paths under docs/, and a link to llms.txt?
