cut: 0.75
evidence: README.md
fitted: met>=0.85, no in-state negative (state: transcript + file evidence; .orly at the repository root)

Look at the `README.md` entry under `project.files`, which is the file's real current content. Does it open with an ASCII-art owl drawing that includes the word oRly?
